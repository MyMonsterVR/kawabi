<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".app.KawabiApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Kawabi"
        android:supportsRtl="true">

        <activity
            android:name=".app.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Kawabi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Browser OAuth redirects (Settings -> Tracking services), each
                 registered against kawabi's own tracker app (planning/TODO.md).
                 Hosts must match the trackers' BrowserOAuthTracker.redirectHost. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="kawabi" android:host="myanimelist-auth" />
                <data android:scheme="kawabi" android:host="anilist-auth" />
            </intent-filter>
        </activity>

        <!-- Auto-launches the app after a sideloaded update finishes installing, so the
             user doesn't need a second tap on the installer's "Open" button. -->
        <receiver
            android:name=".app.update.AppUpdateReplacedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager's own service, needs the type it starts with (setForeground in
             AppUpdateDownloadWorker) declared here too: targetSdk 34+ enforces this at
             the manifest level, a runtime-only ServiceInfo type throws. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Exposes the downloaded update APK (externalCacheDir) as a content:// URI
             for the system installer, required since Android N (a file:// URI would
             throw FileUriExposedException). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
